라이브러리 버전 업데이트에 따른 에제 변경 안내

23차시 1페이지 코드

pandas_datareader 의 버전 변경으로

sdf = pdr.get_data_yahoo(005930.KS, star=datetime(2017,1,1))

코드 수행시 오류가 발생할 수 있습니다.

오류가 발생한다면 다음 코드를 사용해주시기 바랍니다.

import yfinance as yf
sdf = yf.download(005930.KS, start=datetime(2017,1,1))